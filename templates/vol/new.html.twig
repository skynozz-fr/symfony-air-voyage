{% extends 'base.html.twig' %}

{% block title %}Nouveau Vol{% endblock %}

{% block body %}
    <div class="container">
        <div class="row justify-content-center">
            <div class="col-md-6">
                <div class="card mb-5">
                    <div class="card-header">
                        <h3 class="card-title">Nouveau vol</h3>
                    </div>
                    <div class="card-body">
                        {{ include('vol/_form.html.twig') }}
                    </div>
                    <div class="card-footer text-center">
                        <a href="{{ path('app_vol_index') }}" class="btn btn-secondary">Retour à la liste</a>
                    </div>
                </div>
            </div>
        </div>
    </div>
{% endblock %}